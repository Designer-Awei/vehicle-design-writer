# 工作流

## 风格提取

创建博主 → 导入 txt/md/rtf 文件夹 → 单文件容错解析 → 单篇分析 → 多篇聚合 DNA → 多场景模板 → few-shot 短片段 → DNA 质量审查 → 写入风格库。

## 文案生产

选题 / 草稿 / 平台 / 时长 / 风格 / 参考图 → 视觉观察（按图片 hash + 模型 + prompt 版本缓存）→ PFDBI（同样可缓存）→ Base Draft → 模板匹配 → Style Adapter → 质检 → 可编辑成稿与版本。
